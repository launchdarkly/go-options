name: build

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4.1.0
    - uses: actions/setup-go@v5
      with:
        go-version: '1.15'
        check-latest: true
    - run: pip install pre-commit
    - run: make test
